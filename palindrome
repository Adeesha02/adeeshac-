question : Write a C program to find whether the given number is Palindrome or not.

code:

#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int x=n;
    int r,sum;
    while(n!=0)
    {
        r=n%10;
        n=n/10;
        sum=sum*10+r;
    }
    if(x==sum)
    {
        cout<<"Same";
    }
    else
    {
        cout<<"Not Same";
    }
   
   
}
